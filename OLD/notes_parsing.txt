0 - verif pipe aux bouts et quotes non fermee
	peux peut etre verifier les <> colle aussi ? (plus simple apres)

t_cmd_lst	*cmd_lst;
t_cmd_lst	*head;
head = cmd_lst;

1 - split_1 par pipe (en ignorant si quotes)  ✅

2 - while (split_1[i])
		- if (has_env_var() == 0)                           ✅
			split_1[i] = sub_env_var(split_1[i], env);      ✅
		- *lst_split_2 = lst_split_quotes(split_1[i], ' ');  (split par espaces dans une nvl liste)  ✅
		tmp = lst_split_2;
		while (tmp)           🔽🔽🔽🔽
			- if (has_redirections(tmp->content) == 0)
				lst_split_redirections(&tmp);  ->split content (NOT IF REDERIC IN QUOTES)
			- if (has_quotes(tmp->content) == 0)  -> apres redir car <"<"infile pose pb
				remove_quotes(&tmp);  ->update content  //en fait doit verif format redirec d'abord
			tmp = tmp->next
		- verif format des redirec dans lst_split_2 (en enchainant les content)
		- if (ok) 
			cmd_lst = fill_cmd_lst(lst_split_2);
		  else
		  	free tout et abandonne ce prompt
		cmd_lst = cmd_lst->next;
		i++;


---> Liste chaine quasi oblige, trop galere sinon
	doit faire des split linked list surement

